[package]
name = "vime-fcitx5"
version = "0.1.0"
edition = "2021"
description = "Rust backend ABI for the Vietnamese Fcitx5 adapter"
license = "MIT"

[lib]
crate-type = ["rlib", "cdylib"]

[dependencies]
vime-engine = { path = "../engine" }
