# Vietnamese Neovim Integration

Independent Neovim integration project. The `lua/vietnamese` module is the
Neovim-facing entry point, while the optional Rust library can reuse
`../engine` without introducing Neovim dependencies into the engine.

This project is intentionally separate from the Fcitx5 adapter.
