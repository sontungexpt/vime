[package]
name = "vietnamese-nvim"
version = "0.1.0"
edition = "2021"
description = "Rust support library for the Vietnamese Neovim integration"
license = "MIT"

[dependencies]
vietnamese-engine = { path = "../engine" }
